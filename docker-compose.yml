services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./photos:/app/photos
      - ./output:/app/output
      - ./state.json:/app/state.json
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
